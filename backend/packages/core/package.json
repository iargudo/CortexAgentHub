{
  "name": "@cortex/core",
  "version": "1.0.0",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "dev": "ts-node-dev --respawn --transpile-only src/index.ts",
    "start": "node dist/index.js",
    "clean": "rm -rf dist",
    "test": "jest",
    "lint": "eslint src --ext .ts"
  },
  "dependencies": {
    "@cortex/channel-adapters": "workspace:*",
    "@cortex/llm-gateway": "workspace:*",
    "@cortex/mcp-server": "workspace:*",
    "@cortex/shared": "workspace:*",
    "p-queue": "^8.0.1",
    "pg": "^8.16.3",
    "pino": "^8.16.2"
  },
  "devDependencies": {
    "@types/jest": "^29.5.8",
    "@types/node": "^20.10.0",
    "@types/pg": "^8.15.5",
    "jest": "^29.7.0",
    "ts-jest": "^29.1.1",
    "ts-node-dev": "^2.0.0"
  }
}
